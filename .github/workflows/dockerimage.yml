name: Docker Image CI

on: [push]

jobs:

  build:
 
    runs-on: ubuntu-latest
 
    steps:
    - uses: actions/checkout@v1
    
    - name: Build image
      uses: actions/docker/cli@master
      with:
        args: build -t qautomatron/docker-android .
      
